package com.watnapp.etipitaka

import android.app.Activity

/** Minimal empty Activity used only to obtain a real Activity context in
 *  instrumented tests (e.g. confirmAndDownloadDatabase requires one). */
class TestHostActivity : Activity()
